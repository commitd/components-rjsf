import React, { ComponentProps } from 'react'
import { Story, Meta } from '@storybook/react'
import { {{ name }} } from '.'

export default {
  title: 'Components/{{ name }}',
  component: {{ name }},
} as Meta

export const Default: Story<ComponentProps<typeof {{ name }}>> = (args) => <{{ name }} {...args} />

export const Primary = Default.bind({})
Primary.args = {
}
